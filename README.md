# fizbuzz

#### Checkout `dev` branch
```
$ git checkout dev
```

#### Getting started with Laravel
```
$ cp .env.example .env
$ composer install
$ php artisan key:generate
$ php artisan serve
```

#### Getting started with Vue
```
$ npm install
$ npm run dev
$ npm run watch
```
